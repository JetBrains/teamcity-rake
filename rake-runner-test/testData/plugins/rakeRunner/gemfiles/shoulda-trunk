source :rubygems

gem "rake"

#if (RUBY_VERSION.to_f == 1.9) then
#  gem "minitest"
#  gem "minitest-reporters"
#  gem "win32console"
#else
#gem "test-unit"
#end

if (RUBY_VERSION.to_f != 1.8) then
  gem "test-unit"
end

gem "shoulda"
